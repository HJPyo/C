#include<stdio.h>
#include<vector>
using namespace std;

int n, m;
vector<vector<int> >v(200001,vector<int>());

int main()
{
	scanf("%d %d", &n, &m);
	for(int i = 0, a, b; i < n; i++){
		scanf("%d %d", &a, &b);
		
	}
}
